namespace Uptime.Monitoring.Model.Models {
    public enum ContactType {
        EMAIL = 1,
    }

    public enum NotificationType {
        UPDOWN = 1,
        ONLYUP = 2,
        ONLYDOWN = 3
    }
}
